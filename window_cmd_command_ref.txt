사실 경로 타이핑해서 이동하는건 존나 번거로운데 윈도우 ui 환경에서 굳이 그럴필요없으므로
해당 디렉토리로 탐색기로 이동 - 주소창에 "cmd." 이게 걍 효과적임
점하나는 현재 디렉토리이므로 여기서 cmd 열어라 명령어

환경변수
    환경변수에 등록되있어야 cmd에서 그 프로그램 쓸수있음
    환경변수 추가하는 법
    시스템설정 - 고급시스템설정 - 시스템속성 - 환경변수 버튼 클릭
    그럼 창이 뜨는데 sachi에 대한 사용자 변수랑 시스템 변수 이렇게 두개 있다
    걍 별 상관없고 스크롤 내려서 Path -> 여기 리스트에 경로가 적혀있는 프로그램만 cmd로 실행할수있다
        ex)git은 시스템변수에 path에 있음
        실제로 path에 적힌 git.exe 경로 바꾸면 cmd에 이렇게 뜬다
            'git'은(는) 내부 또는 외부 명령, 실행할수있는 프로그램 또는 배치파일이 아닙니다.


Alt+ space ,E,L
 scroll enable//
 하고 나면 방향키로 cmd 스크롤 가능

 list 보기 
  ls가 아니라 dir

 change directory
    동일하게 cd

화면정리 깨끗하게
    cls

디렉토리 만들기/삭제
    mkdir, md
    rmdir, rd

파일 삭제
  rm이 아니라 del
  
파일 이동
    mv 가 아니라 move

경로보기(pwd같은거)
    path 근데 존나 복잡해서 안쓴다

notepad
    메모장실행

dir /a로 숨김파일까지 다 볼수있다(all)

윈도우에서 이름변경할때 확장자가 생략되어있는경우 주의(ex: test.py.py)

