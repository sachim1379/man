크롬 javascript console 단축키
ctrl shift j

콘솔은 한줄씩만 입력할수있다.

Html은 css와 js 를 불러오는 glue(접착제) 이다.

3개의 이름은 각각 app.js // style.css // index.html 이 기본이다.

vs code에서 실행할때는 js파일을 실행하면 안되고 index.html을 실행시켜야한다.
잘못해서 app.js를 실행했으면 .vscode 폴더의 config를 삭제하고 다시 index.html로 실행하면된다.

<변수선언>
var let const 이렇게 세개 있는데, let은 es6에서 추가된 문법이다.
var은 이미 선언된 변수를 다시 선언할수있는데, 이러면 코드 가독성이 떨어지므로
let을 사용하는것이다.
원래는 var만 있었다.

<boolean>
false는 false라는 값이 있는거다
null은 아얘 값이 안존재하는거
undefine 정의만되고 값이 안존재함. 컴퓨터 메모리에는 존재하지만 값이 없음.
null은 자연발생하지 않는다. 즉 비어있다는것을 의도적으로 알려줌.

null

<array>
자바스크립트의 배열은 대괄호 []를 사용한다
쉼표로 구분한다.
자료형이 없고 number와 문자열이 같이 들어간다
vscode에서는 블록선택하고 대괄호를 한번만 쳐도 묶어줘서 편하다.

<comment 주석>
app.js 에서는 //를 사용한다.

<Object>
css와 문법이 똑같다.
const로 선언하거나 let으로 선언할수있다.
중괄호 {} 로 묶고, 쉼표로 구분한다
프로퍼티와 값은 콜론(:)으로 연결한다
끝날때 세미콜론을 넣어준다.
세미콜론이 없어도 문법오류나지는 않는다.

프로퍼티는 업데이트할수도 있다.
객체를 const로 선언해도, 객체프로퍼티는 추가하거나 업데이트할수있다.

콘솔은 오브젝트이다
콘솔을 콘솔로그할수있다

array 선언

function

객체의 메소드 선언
    괄호가 들어가는 순서가 바뀐다.

function 에 argument보다 많은걸 넣어도 괜찮다.
있는거만 들어간다.

열려있는 크롬창을 끄지않고 그냥 새로고침만 해도 디버그할수있다.

무언가를 배울때는 만들면서 배우는게 제일 중요하다. 시험을 보기위한 공부는 시험을 보면 사라지지만 내가 만든것은 사라지지않기
때문이다.

function 내부에서 return하면 함수가 종료되어 그 이후의 코드는 실행되지않는다.

조건문은 영어로 conditional 이다.