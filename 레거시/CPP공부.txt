
ctrl F5 디버그없이 실행
그냥 F5 디버그하고 실행(오래걸림)

std::cout 
std에 사는 cout

iostream
    헤더파일
    C++표준라이브러리가 저장된 파일을 헤더파일 이라고함.
    h가 없는 이유는 옛날꺼랑 구분할라고
    그니까 h가 붙은건 옛날 라이브러리임
    
2021/7/4

std
    <<는 연산자 오버로딩으로 만든 기능임
    옆으로 쳐 넣는다

함수를 쓸라면 메인함수 전에 정의되있어야한다 
뒤에 있으면 오류 발생

함수오버로딩
    이름같아도 인수다르면 다른함수여~

#include <헤더파일>
헤더파일 안에 네임스페이스
네임스페이스 안에  함수정의
    iostream을 인클루드 하지않으면 using namespace std를 쓸수가없다

디버그랑 릴리즈 차이
    릴리즈는 디버그정보가 없어서 빠름
    디버그는 디버그할수있는대신 느림

메모리 종류
낮은주소부터 높은주소 순서대로
코드 데이터 힙(정적,컴파일할때 이미 크기 결정) 스택(동적, 프로그래머가 관리)


&주소연산자
* 역참조연산자, 주소앞에다가 쓰면 그 주소를 클릭

포인터
    포인터를 쓰는 이유
    변수의 주소값을 변수에 저장하고싶을때 int를 써도 안된다하고
    double을 써도 안된다고함
    애초에 주소의 자료형은 주소라서 포인터형 변수에만 집어넣을수있다

꿀팁
    1234로 순서를 정했는데 앞에다가 추가하고싶으면 01 02를 쓰면된다.
    